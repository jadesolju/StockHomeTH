[14/09/2026]
บัคที่พบ
- **การเรียกใช้งาน React Hooks ตามเงื่อนไข (Conditional Hooks Call Violation):** ในไฟล์ `src/components/client/HeaderClientNav.tsx`, `src/components/ui/IosMarkdownRenderer.tsx` และ `src/components/server/DigestHeaderBannerServer.tsx` มีการเรียกใช้งาน Custom Hooks และ React Hooks (`useMemo`, `useState`, `useEffect`, `useRef`) หลังเงื่อนไขการ `return` หรืออยู่ภายในเงื่อนไข ซึ่งละเมิดกฎ Rules of Hooks ของ React อาจส่งผลให้แอปพลิเคชันล่มหรือเกิด State สับสนในขณะ Re-render
- **การอัปเดต State โดยตรงภายใน useEffect (Synchronous State Update in Effects):** พบใน `src/components/client/StockExplorerClient.tsx`, `src/components/client/AdminBackofficeClient.tsx`, และ `src/components/client/PwaRegisterClient.tsx` มีการเรียก `setState` โดยตรงแบบ Synchronous ภายใน `useEffect` ซึ่งทำให้เกิด Cascading Renders ซ้ำซ้อนและกระทบต่อประสิทธิภาพการประมวลผลของ Component
- **การระบุ Dependency Array ไม่ครบถ้วนใน useEffect (Missing Effect Dependencies):** พบใน `src/components/client/StockExplorerClient.tsx` และ `AdminBackofficeClient.tsx` มีการใช้งานตัวแปรหรือฟังก์ชันภายนอก (`activeStockModal`, `setActiveStockModal`, `dbSearch`) แต่ไม่ได้ใส่ใน Dependency Array ทำให้เสี่ยงต่อการเกิด Stale Closure หรือการประมวลผลข้อมูลไม่อัปเดต
- **ตัวแปรและ Import ที่ไม่ได้ใช้งานตกค้างในระบบ (Unused Variables & Imports):** ในหลาย Component เช่น `StockExplorerClient.tsx`, `SyncLogModal.tsx`, `PwaRegisterClient.tsx` มีการ Import ไอคอนและประกาศตัวแปร เช่น `isPending`, `totalStats`, `Filter`, `Clock`, `Sparkles` ที่ไม่ได้นำมาใช้งานจริงในหน้าจอ
- **ความเสี่ยงการ Build ล้มเหลวเมื่อขาดสภาพแวดล้อม Supabase (Missing Build-time Supabase Env Vars):** ในกระบวนการ `next build` หากระบบ CI/CD ไม่ได้ตั้งค่า `NEXT_PUBLIC_SUPABASE_URL` หรือ `NEXT_PUBLIC_SUPABASE_ANON_KEY` หน้า prerender จะไม่สามารถติดต่อ Database และทำให้ Build ล้มเหลว

วิธีการแก้ไขข้อแนะนำ
- **ย้ายการเรียกใช้งาน React Hooks ขึ้นมาไว้ส่วนบนสุดของ Component:** สำหรับ `HeaderClientNav.tsx`, `IosMarkdownRenderer.tsx`, และ `DigestHeaderBannerServer.tsx` ให้ย้ายการเรียก Hooks ทั้งหมด (`useMemo`, `useLanguage`, `useTheme`, `useState` ฯลฯ) ขึ้นมาไว้ก่อนหน้าเงื่อนไข `if (!content) return null;` หรือ `if (...) return;` เพื่อให้ Hook มีลำดับการทำงานที่แน่นอนในทุกๆ Render
- **ปรับปรุงการจัดการ State ใน useEffect หรือย้าย Logic ไปยัง Render Cycle:** คำนวณค่าจาก State/Props โดยตรงในระหว่าง Render แทนการเก็บใน State ใหม่ หรือปรับเงื่อนไขใน `useEffect` ให้ตรวจสอบค่าเปลี่ยนแปลงอย่างรัดกุมก่อนเรียก `setState` เพื่อป้องกัน Cascading Renders
- **เติม Dependency Array ให้ครบถ้วนหรือใช้ useCallback / Functional State Update:** เพิ่มตัวแปรที่ใช้งานจริงเข้าใน Dependency Array ของ `useEffect` หรือใช้ Functional Updates `setLocalExtraStocks((prev) => ...)` และ `useCallback` เพื่อลดความจำเป็นในการฟังฟังก์ชันภายนอก
- **ทำความสะอาด Codebase (Clean Up Dead Code):** ลบ Unused Imports และ Unused Variables ตามที่ Linter (`oxlint`) แจ้งเตือน เพื่อลด Bundle Size และช่วยให้การทำ Code Maintenance เป็นไปได้อย่างมีประสิทธิภาพ
- **ตั้งค่า Fallback Environment Variables หรือ Mock Client สำหรับ Build Process:** ปรับปรุง `src/lib/supabase/client.ts` หรือ `server.ts` ให้มี Fallback Dummy Keys ในกรณี Build Mode หรือระบุ Supabase Environment Variables ใน CI/CD pipeline เสมอ
